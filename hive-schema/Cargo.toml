[package]
name = "hive-schema"
description = "Takes db struct and generates schema.toml file"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
toml = "0.9.1"
